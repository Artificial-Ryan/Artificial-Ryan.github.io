source "https://rubygems.org"

# This gem is used by GitHub Pages to bundle all the necessary Jekyll plugins and versions.
# Adding it here ensures that Netlify's build environment will match GitHub Pages' environment.
gem "github-pages", group: :jekyll_plugins
